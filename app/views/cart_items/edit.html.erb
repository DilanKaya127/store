<%= form_with model: cart_item, method: :put, local: true do |form| %>
  <%= form.number_field :quantity, value: cart_item.quantity, min: 1 %>
  <%= form.submit "Güncelle", class: "btn" %>
<% end %>
